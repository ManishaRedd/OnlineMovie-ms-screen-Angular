//It is same as scree.java class,in order to access the fields we use this
//It should have same fields as screen java class
export class Screen{

    screenId : number;
    screenName : string;
    rows : number;
    columns : number;
    theaterId : number;
  
}
